---
name: streamlit-assessment
description: Inventory a Streamlit instance and produce a migration-readiness readout — environment counts, content mix, complexity, and a value/cost-ranked shortlist. Read-only.
---

# Streamlit migration assessment (read-only)

> SCAFFOLD. Assessments never write to the source or post to Sigma. Produce the
> standard readout (see other `*-assessment` skills) and hand off to
> `streamlit-to-sigma`.

## Phase 0 — Connect
TODO: auth to Streamlit (read-only).

## Phase 1 — Inventory
TODO: enumerate dashboards/models/users; dedup with `scripts/dup-dashboards.py`.

## Phase 2 — Score + shortlist
TODO: complexity score + value/cost-ranked migration shortlist + readout.
